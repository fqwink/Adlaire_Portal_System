# Adlaire Portal System - マスター仕様書

本ドキュメントは Adlaire Portal System の**唯一の仕様の正**（Single Source of Truth）です。
機能仕様・データモデル・API仕様・画面仕様・セキュリティ仕様など、システムに関するすべての仕様はこのファイルに記載します。
他のドキュメント（README等）やコード中のコメントで仕様について触れる場合も、詳細はこのファイルを参照してください。
仕様を変更する場合は、実装の変更とあわせて必ず本ファイルを更新すること。

- **システム名**: Adlaire Portal System
- **バージョン**: v0.21
- **最終更新**: 2026-09-03

---

## 1. システム概要

Deno + TypeScript + SQLiteデータベースで動作する社内ポータルシステム。
社内でよく使うリンクをカテゴリ別に整理して一覧表示する「閲覧画面」と、その内容を編集する「編集画面」の2画面で構成される。
設定データ（タイトル・テーマカラー・お知らせ・カテゴリ・リンク）は SQLite データベースに保存され、
サーバーを起動しておけば複数のブラウザ・端末から同一のデータを参照できる。

**すべてのソースコードはTypeScript(`src/`配下)が正本であり、実行時に使用するJavaScript(`dist/`・`public/js/`配下)は
ビルド（`deno task build`）によってTypeScriptから生成される。生成されたJavaScriptを直接編集してはならない。**
バックエンド・フロントエンドを問わず、新しいコードは必ずTypeScriptで書き、ビルドでJavaScriptを生成する方式に統一する。

**バックエンドの実行環境は、ビルドで生成されたJavaScript（`dist/server.js`）である。** 本番運用（`deno task start` /
`deno task serve`）は必ず `dist/server.js` を実行し、TypeScriptソース（`src/server.ts`等）を直接実行しない。
`deno task dev` は `src/server.ts` を直接実行するが、これは開発時にビルド待ちなしで動作確認するための利便機能に
過ぎず、本番運用の方式ではない。

データベースの初期値（初回起動時のシードデータ）は `src/portal-config.json` というJSONファイルで管理する。
これは「唯一の設定の正本」ではなく「初期値」である点に注意（詳細は §3、§4）。

### 1.1 対象範囲
- 閲覧画面（`public/portal.html` + `src/client/portal.ts`）
- 編集画面（`public/edit.html` + `src/client/edit.ts`）
- バックエンドサーバー（`src/server.ts`）
- データアクセス層（`src/db.ts`）
- バリデーション（`src/validate.ts`）
- 共有型定義（`src/types.ts`）
- 初期シードデータ（`src/portal-config.json`）
- ビルド前チェックスクリプト（`scripts/check-config.ts`）

### 1.2 非対象範囲（現時点で実装しない）
- ユーザー認証・アクセス制御
- 「誰が変更したか」を記録する変更履歴（§4.7の変更履歴は「いつ・どんな内容に変わったか」のみを記録し、
  変更者の識別は行わない。認証機能がないため識別する手段自体が存在しない）

複数ユーザーの同時編集については、`settings.version`によるバージョン番号を用いた楽観的排他制御（§4.2）を実装している。
自分が読み込んだ後に他の人が先に保存していた場合、`PUT /api/config`は`409`を返し上書きを拒否する（誰の変更を残すかを
自動でマージする機能ではなく、後から保存しようとした側に競合を通知し、最新内容を再取得させる方式）。

### 1.3 設計制約: LocalStorage等のブラウザ内永続化の禁止
**LocalStorage・sessionStorage・IndexedDB・Cookie等、ブラウザ内にデータを永続化する仕組みに依存する仕様・機能は、
理由の如何を問わず一切禁止とする。** 閲覧者ごとの個人的な状態（お気に入り、閲覧者個人を識別するクリック履歴、
閲覧設定の記憶等）をブラウザ内保存で実現する機能は提案・実装しない。

なお、§4.6のクリック集計は「誰が・どの端末でクリックしたか」を一切保持しない、URL単位の匿名カウンター
（サーバー側・DBのみ）であり、この制約に抵触しない実現例である。同様にカテゴリの折りたたみ表示（§5.1.2）は
DOM上の一時的な表示状態であり、保存・永続化を行わない（ページを再読み込みすると展開状態に戻る）ため、
この制約の対象外である。

理由: バックエンド（SQLiteデータベース）は存在するが、これは「編集者が管理する共有データ」を保存する場所であり、
「閲覧者個人の状態」を保存する場所ではない。閲覧者ごとの認証・セッション管理を新たに導入しない限り、
サーバー側で個人を識別する手段もない。ブラウザ内保存を許すと、閲覧者ごと・端末ごとに異なる「隠れた状態」が生まれ、
「データベースを見れば全ての表示内容が再現できる」という単純さが失われる。新機能を検討する際は、この制約を
先に確認し、ブラウザ内保存なしで実現できる形に限定すること。

考え方: 「お気に入り」等、閲覧者ごとの個人状態に見える要件でも、**編集者があらかじめデータベースに指定する、
全閲覧者共通の静的データ**として代替できる場合がある。機能要件を「個人ごとの状態」から「編集者が管理する共有データ」
に置き換えられないか、まず検討すること。閲覧者ごとの真の個人化がどうしても必要な場合は、認証・セッション管理の
導入を別途検討する（本ファイル未策定）。

### 1.4 バージョニング方針
本システムは**開発版バージョン**と**安定版バージョン**の2種類のバージョン番号を持つ。どちらも共通して、
**重大度（破壊的変更か軽微な修正か）によって桁や増分を使い分けない、単純な積み上げ式の累積連番を採用し、
いかなる理由があってもリセット（巻き戻し・1からの数え直し）はしない。**

#### 1.4.1 開発版バージョン（`v0.N`）
本ファイルの変更を伴うすべての変更（§10 変更履歴の1エントリ）に付与する番号。

- 表記は **`v0.N`**（Nは1から始まる連番）。先頭の`0`は固定とする（将来`1`への切り替えを検討する余地は残すが、
  基準は本ファイル未策定。切り替えてもNはリセットしない）
- Nは変更を加えるたびに1ずつ増加する。1エントリ＝1バージョンの原則で、§10の変更履歴の行数と一致する
- 桁揃え（ゼロ埋め）は行わない（`v0.1`, `v0.2`, … `v0.9`, `v0.10`, `v0.11`, …と続ける）
- 表記箇所: 本ファイル冒頭の「バージョン」欄が正本。`README.md`・`public/edit.html`（バッジ・フッター）・
  `public/portal.html`（フッター）の版数表記は、実装変更と同時に同期して更新すること
- 旧バージョン番号（`1.0`〜`5.4`、SemVer風の`MAJOR.MINOR`表記）からの移行はv0.14（§10）で実施した。
  旧番号への文中参照が残っている場合は誤りであり、発見次第この方針に沿って修正すること

#### 1.4.2 安定版バージョン（`vX.Y`）
**リリース（外部への公開・配布）は安定版のみを対象とする。** 開発版バージョン（§1.4.1）の全エントリが
リリースされるわけではなく、安定していると判断された時点の開発版を選んで安定版として切り出す。
「安定している」の具体的な判断基準は本ファイル未策定であり、別途策定するリリースポリシーで定める。

- 表記は **`vX.Y`**（例: `v1.3`, `v3.20`, `v12.35`）
  - **X = 安定版リリースの通し番号。** 1件目の安定版リリースを`1`、2件目を`2`、…と単純に1つずつ増える連番。
    メジャー/マイナーのような重大度の意味は持たない（開発版と同じ考え方）
  - **Y = そのリリースを切り出した時点の開発版バージョン（§1.4.1）のN。** 開発版のNをそのまま転記する
    （例: 開発版`v0.35`の時点を3件目の安定版として切り出した場合、`v3.35`となる）
- X・Yともに開発版のNと同様、いかなる理由でもリセットしない。Yは開発版のNが単調増加であることから自動的に、
  Xは「リリース済みの安定版の数」であることから自動的に、それぞれ常に増加し続ける
- 同一の安定版リリースの中でX・Yが指す時点がずれることはない（Yは常にそのリリース時点の開発版Nと一致する）
- 現時点ではまだ安定版リリースを1件も出していない（本方針の策定時点。§10には開発版エントリのみが存在する）

### 1.5 リリースポリシー
本システムは社内ネットワーク内の単一環境で運用され（§7.3）、外部への配布・パッケージ公開は行わない。
「リリース」とは、開発版（§1.4.1）の中から安定していると判断した時点を選び、安定版バージョン（§1.4.2）として
記録し、運用者が本番環境（社内サーバー）へ反映することを指す。

#### 1.5.1 安定版と判定する基準
以下をすべて満たす開発版のみを安定版として切り出せる。

- `deno task check` が型エラーなしで通ること
- クリーンな状態（`data/`・`dist/`・`public/js/`を削除した状態）から `deno task build` が成功すること
- 主要導線の動作確認が完了していること（サーバーを実際に起動し、閲覧画面・編集画面の主要機能をクリック等で
  確認する。§5の画面仕様に記載の機能が対象）
- **既知の未修正バグが0件であること。** 不具合を発見した場合は、バックログとして保留・記録するのではなく、
  リリース前提でその場で修正する（「軽微だから後回しにする」という判断はしない。未修正のバグを一覧などで
  追跡・保留する仕組みは持たない）

#### 1.5.2 リリースの手順
1. mainブランチへのマージは、それ自体では自動的にリリースとならない（mainへのマージは開発版の更新にとどまる）
2. 運用者が「この開発版を安定版として出す」と明示的に判断した時点で、その開発版バージョン（v0.N）を元に
   安定版バージョン（vX.Y、§1.4.2）を1つ切り出す
3. §11「安定版リリース履歴」に `vX.Y | 日付 | 対応する開発版N | 内容概要` の1行を追記する
4. 該当コミットに`vX.Y`のgitタグを付与する
5. 運用者が本番環境で`deno task build && deno task start`（またはビルド済み成果物の配置）を実行し、実際に反映する

#### 1.5.3 不具合が見つかった場合
安定版として出した後に不具合が見つかった場合も、既に記録した安定版バージョンの番号を書き換える・差し替える
ことはしない（§1.4のリセット禁止の原則を継承する）。見つかった不具合はリリース前提でその場で修正し、修正は
通常どおり開発版エントリ（§10）として積み上げ、§1.5.1の基準を再度満たすことを確認したうえで、新しい安定版
（Xを1つ進め、その時点の開発版Nを新しいYとする）として改めてリリースする。

---

## 2. システム構成

```
adlaire_portal/
├── deno.json          # Denoタスク定義 (ビルド/起動/開発/型チェック)・コンパイラオプション
├── deno.lock            # 依存関係ロックファイル
├── src/                  # TypeScript正本 (すべての実装はここを変更する)
│   ├── types.ts          # 共有型定義 (PortalConfig等)
│   ├── validate.ts        # 設定データのバリデーション
│   ├── portal-config.json  # 初回起動時のシードデータ (JSON)
│   ├── db.ts               # SQLiteデータアクセス層 (jsr:@db/sqlite)
│   ├── server.ts            # Denoサーバー本体 (静的配信 + REST API)
│   └── client/
│       ├── portal.ts         # 閲覧画面のクライアントロジック
│       └── edit.ts            # 編集画面のクライアントロジック
├── scripts/
│   └── check-config.ts    # ビルド前に portal-config.json (シード) を検証するスクリプト
├── public/
│   ├── portal.html        # 閲覧画面 (HTML/CSSのみ。ロジックはjs/portal.jsを読み込む)
│   ├── edit.html            # 編集画面 (同上、js/edit.jsを読み込む)
│   └── js/                    # ビルド生成物 (自動生成・gitignore対象・直接編集禁止)
│       ├── portal.js
│       └── edit.js
├── dist/
│   └── server.js             # ビルド生成物 (src/server.tsのバンドル。自動生成・gitignore対象)
├── data/
│   ├── portal.db               # SQLiteデータベースファイル (自動生成・gitignore対象)
│   ├── access.log               # アクセスログ・エラーログ (自動生成・gitignore対象。§8)
│   └── backups/                 # DBの定期バックアップ (自動生成・gitignore対象。§2.3)
│       └── portal-<timestamp>.db
├── README.md                  # セットアップ・運用ガイド
└── SPEC.md                    # 本ファイル (マスター仕様書)
```

### 2.1 技術スタック
| 項目 | 内容 |
|---|---|
| 言語（正本） | TypeScript（`src/`配下すべて。strict モード） |
| 実行時生成物 | JavaScript（`deno bundle`によりTypeScriptから生成。`dist/`・`public/js/`配下） |
| 実行環境 | Deno v2.9以上 |
| HTTPサーバー | Deno標準API（`Deno.serve`）+ `jsr:@std/http`（静的ファイル配信） |
| データベース | SQLite（`jsr:@db/sqlite`。FFI経由でネイティブsqlite3を利用） |
| フロントエンド | TypeScriptをビルドしたプレーンJavaScript（フレームワーク不使用） |
| 通信 | フロントエンド ⇔ バックエンド間はJSON REST API (`fetch`) |

### 2.2 ビルド・起動方法
```bash
deno task build   # portal-config.jsonの検証 → src/*.ts から dist/server.js, public/js/*.js を生成
deno task start   # dist/server.js を実行 (既定ポート: 3000)
deno task serve   # build + start をまとめて実行

PORT=8080 deno task start   # ポートを変更する場合
BACKUP_INTERVAL_MINUTES=30 deno task start   # DBバックアップの間隔を変更する場合(既定: 60分。§2.3)
LINK_CHECK_INTERVAL_MINUTES=120 deno task start   # リンクの定期自動チェック間隔を変更する場合(既定: 360分。§2.4)

deno task dev      # ビルドせず src/server.ts を直接実行 + ファイル変更監視 (開発用)
deno task check    # 型チェックのみ実行 (ビルドしない)
deno task validate-config   # portal-config.json (シード) の検証のみ実行
```
初回起動時、`data/portal.db` が存在しなければ自動生成し、`src/portal-config.json` の内容で初期化する
（`src/db.ts` の `ensureSeeded()`）。**2回目以降の起動では `portal-config.json` の内容は参照されない。**
DBが既に存在する状態でこのファイルを書き換えても、明示的に「デフォルトに戻す」操作（§5.2.2）を行わない限り
反映されない。

> ℹ️ `src/client/portal.ts` / `src/client/edit.ts` はブラウザで実行されるコードのため、
> `deno bundle --platform browser --format iife` でプレーンJavaScript（IIFE形式）にコンパイルし、
> `public/js/portal.js` / `public/js/edit.js` として出力する。
> `src/server.ts` / `src/db.ts` はDenoランタイム上で実行されるコードのため、
> `deno bundle --platform deno` で `dist/server.js` に単一ファイルとしてバンドルする。

### 2.3 DBバックアップ
サーバー起動時、および環境変数 `BACKUP_INTERVAL_MINUTES`（既定値: `60`）で指定した分数ごとに、
`data/portal.db` を `data/backups/portal-<ISO8601風タイムスタンプ>.db` としてコピーする（`src/db.ts` `backupDatabase()`）。
`BACKUP_INTERVAL_MINUTES=0` を指定すると定期バックアップを無効化できる（起動時バックアップも行われない）。

- 直近24世代（`MAX_BACKUPS_TO_KEEP`）のみを保持し、それより古いバックアップファイルは削除する
- `Deno.copyFile`によるファイルコピーであり、コピー中に他のリクエストが同時に`portal.db`へ書き込む可能性を
  完全には排除できないベストエフォートな方式。トランザクション整合性を厳密に保証するものではないが、
  本システムの用途（書き込み頻度が低い社内ポータルの設定データ）では実用上のリスクは小さいと判断している
- バックアップ処理自体が失敗しても、標準出力にエラーを出力するのみでサーバーの起動・稼働は継続する
- リストア（バックアップからの復元）は本システムには実装しない。必要な場合は`data/backups/`配下の
  該当ファイルを手動で`data/portal.db`に上書きコピーし、サーバーを再起動すること

### 2.4 リンクの定期自動チェック
サーバー起動時、および環境変数 `LINK_CHECK_INTERVAL_MINUTES`（既定値: `360`＝6時間）で指定した分数ごとに、
その時点で設定に含まれる全リンクのURLへHEAD/GETリクエストを送って到達可否を確認し、結果を
`link_check_status`テーブル（§3.7）へ記録する（`src/server.ts` `runScheduledLinkCheck()`）。
`LINK_CHECK_INTERVAL_MINUTES=0` を指定すると無効化できる（起動時チェックも行われない）。

- 確認方法は手動チェック（`POST /api/check-links`、§4.4）と同じ`checkOneLink()`を使う（HEADが405の場合のみ
  GETで再試行、タイムアウトは5秒）が、結果の保存先・用途が異なる。手動チェックはDBに保存せずその場限りの
  確認（編集画面）、定期自動チェックは`link_check_status`に保存し到達可否のみを閲覧画面のバッジ表示（§5.1.2）
  に反映する
- 全リンクを`Promise.all`で並行して確認する。リンク数が多い場合は外部サイトへの同時接続数が増える点に注意
- チェック処理自体が失敗しても、標準出力にエラーを出力するのみでサーバーの起動・稼働は継続する

---

## 3. データモデル / DBスキーマ

SQLite (`data/portal.db`) に以下8テーブルで正規化して保存する。

### 3.1 `settings`（1行のみ）
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PRIMARY KEY, CHECK(id=1) | 常に1固定（単一設定のみ許容） |
| title | TEXT | NOT NULL | ポータルのタイトル |
| theme_color | TEXT | NOT NULL | テーマカラー（`#rrggbb`形式） |
| version | INTEGER | NOT NULL DEFAULT 1 | 楽観的排他制御用のバージョン番号。`PUT /api/config`で更新するたびに1つ進む（§4.2参照） |

### 3.2 `news`
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | |
| date | TEXT | NOT NULL | 表示用の日付文字列（形式自由、例: `2026/02/14`） |
| text | TEXT | NOT NULL | お知らせ本文 |
| sort_order | INTEGER | NOT NULL | 表示順（昇順。ピン留めされている場合の並び順にも使う。下記参照） |
| pinned | INTEGER | NOT NULL DEFAULT 0 | 常に先頭に表示するかどうか（0/1の真偽値）。編集者が管理する共有データであり、閲覧者個人の状態ではないため§1.3の制約には抵触しない |
| expires_at | TEXT | NULL可 | 有効期限（`YYYY-MM-DD`形式）。この日付を過ぎると閲覧画面（`portal.html`）には表示されなくなる。`NULL`は無期限を表す |

閲覧画面（`GET /api/config`の`news`配列の並び順）は `ORDER BY pinned DESC, sort_order ASC, id ASC` で取得する。
すなわちピン留めされたお知らせは常に非ピン留めのものより前に表示され、ピン留め同士・非ピン留め同士では
従来どおり`sort_order`の昇順（編集画面での並び順）に従う。

### 3.3 `categories`
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | |
| name | TEXT | NOT NULL | カテゴリ名 |
| sort_order | INTEGER | NOT NULL | 表示順（昇順） |
| hidden | INTEGER | NOT NULL DEFAULT 0 | 「下書き」状態かどうか（0/1の真偽値）。`1`の場合、閲覧画面（`portal.html`）には表示されないが、編集画面では引き続き編集できる（§5.1.2, §5.2.2） |

### 3.4 `links`
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | |
| category_id | INTEGER | NOT NULL, REFERENCES categories(id) ON DELETE CASCADE | 所属カテゴリ |
| name | TEXT | NOT NULL | リンク名 |
| url | TEXT | NOT NULL | リンクURL |
| icon | TEXT | NOT NULL | 絵文字アイコン1文字 |
| sort_order | INTEGER | NOT NULL | カテゴリ内での表示順（昇順） |

### 3.5 `link_clicks`
リンクURLごとの匿名クリック回数。個人・端末を識別する情報は一切持たない（§1.3, §4.6）。

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| url | TEXT | PRIMARY KEY | リンクのURL文字列（`links.url`と同じ値） |
| count | INTEGER | NOT NULL DEFAULT 0 | 累計クリック回数 |

`links`行は`PUT /api/config`のたびに全削除・再作成される（§4.2）ため、`links.id`ではなくURL文字列をキーにして、
保存内容（カテゴリ構成やリンクの並び順）が変わっても集計を維持する。同じURLが複数のリンクとして登録されている
場合はカウントを合算して扱う（キーがURLであるため）。

### 3.6 `link_added_at`
リンクURLが最初に保存された日時。閲覧画面のNEWバッジ表示に使う（§5.1.2）。

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| url | TEXT | PRIMARY KEY | リンクのURL文字列（`links.url`と同じ値） |
| added_at | TEXT | NOT NULL | 最初に保存された日時（ISO 8601形式の文字列） |

`link_clicks`（§3.5）と同じ理由でURL文字列をキーにする。`link_clicks`と異なり値は一度記録したら更新しない
（`PUT /api/config`のたびに、まだこのテーブルにないURLのみ現在時刻で新規追加する。`ON CONFLICT(url) DO NOTHING`）。
そのため、同じURLのリンクを削除して後日また同じURLで登録し直しても「最初に登録された日時」のままになる
（意図的な挙動。リンクの区別はURLで行うため）。

### 3.7 `link_check_status`
リンクURLごとの定期自動チェック（§5.1.2）の最新結果。手動チェック（`POST /api/check-links`、§4.4）の
結果はここには保存されない（その場限りの確認のため）。

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| url | TEXT | PRIMARY KEY | リンクのURL文字列（`links.url`と同じ値） |
| ok | INTEGER | NOT NULL | 直近の自動チェックで到達できたかどうか（0/1の真偽値） |
| checked_at | TEXT | NOT NULL | 直近のチェック日時（ISO 8601形式の文字列） |

`link_clicks`・`link_added_at`と同じ理由でURL文字列をキーにする。`link_added_at`と異なり、チェックのたびに
`ok`・`checked_at`を上書きする（`ON CONFLICT(url) DO UPDATE`）。

### 3.8 `change_log`
設定が保存される（`PUT /api/config`・`POST /api/config/reset`が成功する）たびのスナップショット。
「誰が変更したか」は記録しない（§1.2, §4.7）。

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | |
| changed_at | TEXT | NOT NULL | 保存日時（ISO 8601形式の文字列） |
| config_json | TEXT | NOT NULL | 保存後の設定全体（§3.9の論理データ構造）をJSON文字列化したもの |

直近50件（`MAX_HISTORY_ENTRIES`）のみを保持し、それより古い履歴は保存のたびに自動的に削除される。

### 3.9 論理データ構造（API入出力形式）
API（`/api/config`）は上記テーブルを以下のJSON構造に組み立てて返す。編集画面の内部状態（`config`変数）もこの形式に一致する。
`src/portal-config.json`（シードデータ）も同じ形式で記述する。

```json
{
  "title": "Adlaireポータル",
  "themeColor": "#00a968",
  "news": [
    { "date": "2026/02/14", "text": "お知らせ内容", "pinned": true, "expiresAt": "2026-12-31" }
  ],
  "categories": [
    {
      "name": "カテゴリ名",
      "hidden": false,
      "links": [
        { "name": "リンク名", "url": "https://example.com", "icon": "🔍", "clicks": 3, "addedAt": "2026-02-14T00:00:00.000Z", "broken": false }
      ]
    }
  ]
}
```
`clicks`は`link_clicks`テーブル（§3.5）から、`addedAt`は`link_added_at`テーブル（§3.6）から、`broken`は
`link_check_status`テーブル（§3.7）から、それぞれ`GET /api/config`のたびに付与される、サーバー算出の
読み取り専用の派生値である。`clicks`は0件（未クリック）の場合、`addedAt`は該当URLがまだ`link_added_at`に
存在しない場合（通常は初回保存時に必ず記録されるため起こらないはずだが、念のため）、`broken`は到達可能
または未チェックの場合に、それぞれプロパティ自体を省略する（上記の例では説明のため`false`を明示しているが、
実際のレスポンスでは省略される）。いずれも`PUT /api/config`の入力に含めても無視され、対応するテーブルには
一切影響しない（`clicks`は`POST /api/click`（§4.6）でのみ、`addedAt`は初回保存時にのみ、`broken`は定期自動
チェック（§5.1.2）でのみ更新される）。`news[].pinned`（省略時false扱い）・`news[].expiresAt`（省略時無期限）・
`categories[].hidden`（省略時false=公開扱い）は、他のフィールドと同じく編集者が`PUT /api/config`で指定する
通常の入力項目である（読み取り専用ではない）。

### 3.10 共有型定義（`src/types.ts`）
```typescript
interface NewsItem {
  date: string;
  text: string;
  pinned?: boolean;    // 常に先頭に表示する(編集者が設定する共有データ)。省略時はfalse扱い
  expiresAt?: string;  // この日付(YYYY-MM-DD)を過ぎたら閲覧画面に表示しなくなる。省略時は無期限
}

interface LinkItem {
  name: string;
  url: string;
  icon: string;
  clicks?: number;   // サーバーが付与する読み取り専用の匿名クリック数(§3.5, §3.9)。PUTでは無視される
  addedAt?: string;  // サーバーが付与する読み取り専用の初回保存日時(§3.6, §3.9)。PUTでは無視される
  broken?: boolean;  // サーバーが付与する読み取り専用の定期チェック結果(§3.7, §3.9)。PUTでは無視される
}

interface Category {
  name: string;
  links: LinkItem[];
  hidden?: boolean;  // 「下書き」状態(閲覧画面には表示しない)。編集者が指定する通常の入力項目。省略時はfalse扱い
}

interface PortalConfig {
  title: string;
  themeColor: string;
  news: NewsItem[];
  categories: Category[];
}
```

---

## 4. API仕様

すべてJSON形式。ベースパスなし（サーバールート直下）。

### 4.1 `GET /api/config`
現在の設定を取得する。

- **リクエストボディ**: なし
- **成功時**: `200 OK`、上記「論理データ構造」のJSONを返す。レスポンスヘッダー `ETag` に現在の設定バージョン（整数、`"1"`のような文字列表現）を付与する
- **失敗時**: 設定データが存在しない場合 `404 Not Found`、`{ "error": "設定データが見つかりません" }`
  - 通常は初回起動時にシードデータで自動初期化されるため発生しない

### 4.2 `PUT /api/config`
設定全体を置き換える（部分更新は不可。常に全項目を送信する）。楽観的排他制御に対応する。

- **リクエストボディ**: 「論理データ構造」と同じJSON
- **リクエストヘッダー**: `If-Match`（任意）— 直前に`GET`で取得した`ETag`の値をそのまま指定する。指定した場合、サーバー側の現在のバージョンと一致しない（＝自分が読み込んだ後に他の変更が保存された）ときは更新を拒否する。省略した場合はバージョンチェックを行わない
- **処理**: サーバー側で §6 のバリデーションおよび`If-Match`の検証を実施した後、`settings`のバージョンを1つ進めつつ更新し、`news`/`categories`/`links`を全削除してから再挿入する（トランザクション内で実行、失敗時はロールバック）
- **成功時**: `200 OK`、更新後の設定をJSONで返す。レスポンスヘッダー `ETag` に更新後の新しいバージョンを付与する
- **失敗時**:
  - バリデーションエラー時は `400 Bad Request`、`{ "error": "<エラーメッセージ>" }`
  - `If-Match`のバージョンが現在のDBのバージョンと一致しない場合は `409 Conflict`、`{ "error": "<競合を説明するメッセージ>" }`（更新は適用されない）

### 4.3 `POST /api/config/reset`
データベースの内容を `src/portal-config.json` の内容にリセットする（`PUT`と同じ置き換え処理をシードデータに対して実行）。`If-Match`によるバージョンチェックは行わない（確認ダイアログを経た明示的な操作のため）。

- **リクエストボディ**: なし
- **成功時**: `200 OK`、リセット後の設定をJSONで返す。レスポンスヘッダー `ETag` に更新後のバージョンを付与する
- **失敗時**: `500 Internal Server Error`、`{ "error": "<エラーメッセージ>" }`

### 4.4 `POST /api/check-links`
指定されたURL群それぞれについて、サーバーからHEADリクエスト（405が返る場合はGETで再試行）を送信し、到達可否を確認する。
DBへの読み書きは一切行わない、その場限りの確認用エンドポイント。

- **リクエストボディ**: `{ "urls": string[] }`（最大200件。超過時は`400`）
- **成功時**: `200 OK`、`{ "results": [{ "url": string, "ok": boolean, "status": number | null, "error": string | null }] }`（`urls`と同じ順序）
  - `url`がURL検証（§7.2）を満たさない場合は、通信を行わず `ok: false, status: null, error: "URLの形式が不正です"` を返す
  - 通信タイムアウトは5秒。タイムアウト・DNS失敗等のネットワークエラー時は `ok: false, status: null, error: "<エラー内容>"` を返す
- **失敗時**: `urls`が配列でない、または200件を超える場合は `400 Bad Request`、`{ "error": "<エラーメッセージ>" }`

### 4.5 `GET /healthz`
死活監視用のヘルスチェックエンドポイント。DBアクセスは行わない軽量な応答のみ。

- **リクエストボディ**: なし
- **成功時**: `200 OK`、`{ "status": "ok", "uptimeSeconds": number }`（`uptimeSeconds`はサーバープロセス起動からの経過秒数）
- 認証は行わない（§7.3と同様、信頼できるネットワーク内での利用を前提とする）

### 4.6 `POST /api/click`
リンククリックを匿名で1件記録する。閲覧画面（`src/client/portal.ts`）から、リンクをクリックした際に
結果を待たないfire-and-forgetで送信される（§5.1.2）。

- **リクエストボディ**: `{ "url": string }`（§7.2のURL検証を満たす必要がある）
- **処理**: `link_clicks`テーブル（§3.5）の該当URLのカウントを1加算する（存在しない場合は1件目として新規作成）。
  閲覧者・端末を識別する情報（IPアドレス、Cookie、User-Agent等）は一切保存しない
- **成功時**: `200 OK`、`{ "ok": true }`
- **失敗時**: `url`が未指定またはURL検証を満たさない場合は `400 Bad Request`、`{ "error": "<エラーメッセージ>" }`
- このエンドポイントの成否はUIに反映されない（クライアント側は送信結果を無視する。§5.1.2）

### 4.7 `GET /api/history` / `GET /api/history/:id`
変更履歴（`change_log`テーブル、§3.8）を参照する。「いつ・どんな内容に変わったか」のみを記録し、
「誰が変更したか」は記録しない（§1.2）。

- **`GET /api/history`**: 履歴一覧を新しい順に返す。内容（設定全体のスナップショット）は含まない軽量な一覧
  - **成功時**: `200 OK`、`{ "entries": [{ "id": number, "changedAt": string }] }`
- **`GET /api/history/:id`**: 指定した`id`の履歴1件の詳細（保存日時と、保存当時の設定全体のスナップショット）を返す
  - **成功時**: `200 OK`、`{ "changedAt": string, "config": <§3.9の論理データ構造> }`
    - `config_json`は`PUT /api/config`受信時のリクエストボディをそのまま保存したものであるため、含まれる
      `categories[].links[].clicks`は「保存操作を行った時点でクライアントが最後に取得していた値」がそのまま
      残っている場合がある（現時点の`link_clicks`テーブルの値に動的に更新されるわけではない）。履歴の内容を
      正確なクリック数の記録として扱わないこと。参照する際はタイトル・お知らせ・カテゴリ・リンクの構成・
      並び順のスナップショットとして利用する
  - **失敗時**: 指定`id`の履歴が存在しない場合は `404 Not Found`、`{ "error": "指定された変更履歴が見つかりません" }`
- 編集画面（§5.2.2）から履歴の内容を編集フォームへ読み込める（「この内容を確認」ボタン）が、**読み込んだだけでは
  DBには反映されない**。反映するには通常の保存操作（`PUT /api/config`）を明示的に行う必要がある

### 4.8 `GET /api/backup/download`
現在の`data/portal.db`ファイルをそのままダウンロードする（編集画面の「💾 DBをダウンロード」ボタンから使用。§5.2.2）。

- **リクエストボディ**: なし
- **成功時**: `200 OK`、`Content-Type: application/octet-stream`、
  `Content-Disposition: attachment; filename="portal-<ISO8601風タイムスタンプ>.db"` でSQLiteファイルのバイト列を返す
- **失敗時**: ファイル読み込みに失敗した場合は `500 Internal Server Error`、`{ "error": "<エラーメッセージ>" }`
- 定期バックアップ（§2.3）と同じく、ファイル読み込み中に他のリクエストが同時に書き込む可能性を完全には
  排除できないベストエフォートな取得である
- 認証を行わないため、このエンドポイントにアクセスできる者は誰でもDB全体（設定データだけでなく
  クリック集計・変更履歴・アクセスログ等も含む）をダウンロードできる。§7.3と同じく信頼できるネットワーク内
  での運用を前提とする

### 4.9 入力サイズ制限
APIリクエストボディは `Content-Length` ヘッダーをもとに最大1MBに制限される（`src/server.ts` `readJsonBody()`）。

---

## 5. 画面仕様

### 5.1 閲覧画面（`public/portal.html`）

#### 5.1.1 画面構成
- **サイドバー**: ロゴ、今日の日付、カテゴリ一覧ナビゲーション（各カテゴリのリンク件数バッジ付き）
- **メインエリア**:
  - エラー表示欄（API取得失敗時のみ表示）
  - お知らせ欄（お知らせが1件以上ある場合のみ表示）
  - 検索ボックス
  - カテゴリ別リンクカードのグリッド表示
  - フッター（バージョン表記）

#### 5.1.2 機能仕様
| 機能 | 仕様 |
|---|---|
| データ読み込み | ページロード時に `GET /api/config` をfetchし、取得したJSONを`renderPortal()`でDOMに描画する。失敗時はエラー欄を表示する |
| ナビゲーション | サイドバーのカテゴリリンクをクリックすると、該当セクションへスムーズスクロールする（アンカーリンクの既定動作は`preventDefault`） |
| お知らせ表示 | `news`配列を日付+本文で一覧表示。0件の場合は欄ごと非表示。リスト部分は最大高さ120pxでスクロール可能。並び順はサーバー側で`pinned`降順→`sort_order`昇順に整列済み（§3.2）のためクライアント側では並べ替えない |
| お知らせのピン留め表示 | `pinned: true`のお知らせには📌アイコンを付け、背景色を変えて強調する。並び順は常に非ピン留めのお知らせより前（サーバー側で整列済み。§3.2） |
| お知らせの有効期限 | `expiresAt`が今日の日付（クライアントのローカル日付、`YYYY-MM-DD`）より前のお知らせは表示しない（クライアント側でフィルタリング。`expiresAt`が未設定の場合は無期限として常に表示） |
| テーマカラー反映 | `themeColor`からCSS変数 `--primary` を設定し、そこから `--primary-light`（10%不透明度相当のrgba）と`--primary-dark`（RGB各値-30）を自動算出してCSS変数に反映する |
| 日付表示 | サイドバーに `YYYY.MM.DD` 形式で今日の日付をクライアント側で計算して表示 |
| リアルタイム検索 | 検索ボックスへの入力（`keyup`）ごとに、リンク名+カテゴリ名（小文字化して結合した`data-search`属性）に検索語を含むリンクのみ表示。該当リンクが0件のカテゴリセクションごと非表示にする。検索語が空の場合は全件表示に戻す |
| 検索結果ハイライト | 検索語にマッチしたリンク名の該当部分を`<mark>`タグで強調表示する（`highlightMatch()`。前後の非マッチ部分・マッチ部分はそれぞれ個別に`escapeHtml()`してから連結するためXSS安全。§7.1）。検索語が空、またはそのリンクが非マッチの場合は通常のエスケープ表示に戻る |
| カテゴリの折りたたみ | カテゴリ見出し（`category-header`）をクリックすると、そのカテゴリのリンク一覧（`grid`）の表示/非表示を切り替える（`collapsed`クラスの付け外し）。**この開閉状態はDOM上の一時的な表示にすぎず、保存・永続化は一切行わない**（ページの再読み込みや別タブでは常に全展開の初期状態に戻る。§1.3参照）。検索でマッチした結果を含むカテゴリは、折りたたまれていても自動的に展開される。サイドバーのナビゲーションリンクをクリックした場合も、遷移先のカテゴリが折りたたまれていれば自動的に展開してからスクロールする |
| リンククリック | 新規タブ（`target="_blank"`、`rel="noopener noreferrer"`）でURLを開く |
| クリック数の匿名集計 | リンクをクリックすると、`POST /api/click`（§4.6）へそのURLを結果を待たずに送信する（`reportClick()`。fire-and-forgetで、失敗してもUIには影響しない）。閲覧者・端末を識別する情報は送信しない。サーバーから取得した`clicks`（1以上の場合のみ）をリンクカードの右上に「👁 <回数>」として表示する |
| NEWバッジ | サーバーから取得した`addedAt`（§3.6, §3.9）が現在時刻から`NEW_BADGE_DAYS`（既定7日）以内であるリンクには、カードの左上に赤い「NEW」バッジを表示する。`addedAt`が7日より前、または存在しない場合は表示しない |
| 到達不可バッジ | サーバーから取得した`broken`（§3.7, §3.9）が`true`のリンクには、カードの左上に⚠️バッジを表示する（NEWバッジと並べて表示される）。サーバー側の定期自動チェック（§2.4）で到達できなかったことを示す |
| 下書きカテゴリの非表示 | `hidden: true`のカテゴリ（§3.3, §3.9）は、サイドバーのナビゲーションにも、コンテンツエリアのセクションにも一切表示しない（存在しないものとして扱う） |
| ダークモード | `prefers-color-scheme: dark` により自動的に配色を切り替える（手動切り替えは未実装。§1.3参照） |
| レスポンシブ | 画面幅768px以下でサイドバーが横並びのスクロール可能なタブ形式に変化する |

### 5.2 編集画面（`public/edit.html`）

#### 5.2.1 画面構成
- **左ペイン（エディタパネル）**: 幅可変（ドラッグでリサイズ可能、最小300px）
  - ヘッダー: タイトル、バージョンバッジ、DB接続ステータス表示
  - フォームエリア: 基本設定・お知らせ・カテゴリ/リンクの編集フォーム
  - フッター: 元に戻す/やり直す・保存・エクスポート・インポート・リンクチェック・変更履歴・一括インポート・
    アクセス統計・名前順に並び替え・DBダウンロード・リセットの各ボタン
- **右ペイン（プレビューパネル）**: `iframe`によるライブプレビュー（PC表示/スマホ表示のトグル切り替え可）
- **変更履歴モーダル**（`#history-modal`）: 「📜 変更履歴」ボタンで開くオーバーレイ。通常時は非表示
- **一括インポートモーダル**（`#bulk-import-modal`）: 「📋 一括インポート」ボタンで開くオーバーレイ。通常時は非表示
- **アクセス統計モーダル**（`#stats-modal`）: 「📊 アクセス統計」ボタンで開くオーバーレイ。通常時は非表示

#### 5.2.2 機能仕様
| 機能 | 仕様 |
|---|---|
| 初期読み込み | ページロード時に `GET /api/config` を非同期fetchし、`config`変数に格納。レスポンスの`ETag`ヘッダーを`currentVersion`として保持する。取得完了までフォームは「読み込み中...」表示、保存ボタンは無効化。失敗時はステータス欄にエラー表示 |
| 基本設定編集 | タイトル（テキスト入力）、テーマカラー（カラーピッカー）を編集すると、即座に`config`とプレビューに反映（自動保存はされない） |
| お知らせ管理 | 追加（先頭に本日日付で追加）・削除・日付/本文のインライン編集・ピン留めの切り替え・有効期限の設定 |
| お知らせのピン留め | 各お知らせ行の📌ボタンで`pinned`を切り替える（トグル式。押すたびに反転）。ピン留め中は📌ボタンを強調表示する。保存すると閲覧画面で常に先頭に表示される（§5.1.2, §3.2） |
| お知らせの有効期限設定 | 各お知らせ行の日付入力（`type="date"`）で`expiresAt`を設定する。空欄にすると無期限に戻る。保存するとこの日付を過ぎた時点で閲覧画面から自動的に非表示になる（§5.1.2, §3.2） |
| カテゴリ管理 | 追加（末尾に「新規カテゴリ」を追加）・削除（確認ダイアログあり）・名称編集・並び替え（⬆️⬇️ボタン、またはドラッグ&ドロップ）・下書き状態の切り替え |
| カテゴリの下書き状態 | 各カテゴリの👁️/🙈ボタンで`hidden`を切り替える（トグル式）。下書き中（🙈）はカテゴリの箱全体を半透明表示し「🙈 下書き中」のラベルを添える。保存すると閲覧画面には表示されなくなるが、編集画面・ライブプレビューでは引き続き確認・編集できる（§3.3, §5.1.2） |
| リンク管理 | 追加（カテゴリ末尾に空リンクを追加）・削除・名称/URL編集・並び替え（⬆️⬇️ボタン、またはドラッグ&ドロップ）。クリック数が1以上あるリンクには「👁 <回数>」を読み取り専用で表示する（§3.5, §4.6由来。編集不可） |
| ドラッグ&ドロップ並び替え | 各カテゴリの見出し・各リンク行の左端にあるハンドル（⠿）をドラッグし、別のカテゴリ/同一カテゴリ内の別リンク行の上でドロップすると、その位置に並び替える（HTML5 Drag and Drop APIのネイティブ機能を使用。`onCatDragStart`/`onCatDragOver`/`onCatDrop`、`onLinkDragStart`/`onLinkDragOver`/`onLinkDrop`）。リンクは**同一カテゴリ内の並び替え・カテゴリをまたいだ移動の両方**に対応する。カテゴリ本体（箱全体）へのドロップは「そのカテゴリの末尾へ移動」を意味し、リンクが1件もない空のカテゴリへ移動する手段にもなる。既存の⬆️⬇️ボタンによる並び替え（同一カテゴリ内のみ）と併存する代替手段であり、いずれも保存（「💾 保存」）するまでデータベースには反映されない |
| 一括インポート | 「📋 一括インポート」ボタンでモーダルを開き、追加先カテゴリを選択した上で、テキストエリアに「名称, URL」形式（1行1件、カンマ区切り。表計算ソフトからの貼り付けを想定したタブ区切りにも対応）で複数行貼り付けて「＋ まとめて追加」を押すと、選択したカテゴリの末尾へまとめてリンクを追加する（アイコンは既定で📄になる）。解析できない行（区切り文字がない、名称またはURLが空）は読み飛ばす。**この時点ではDBには反映されない**。反映するには別途「保存」を押す必要がある。URLの妥当性チェックは保存時のサーバー側バリデーション（§6）に委ねる |
| 元に戻す/やり直す（Undo/Redo） | フッターの「↩️ 元に戻す」「↪️ やり直す」ボタン、またはキーボードショートカット（`Ctrl+Z` / `Ctrl+Shift+Z`。Macでも`Cmd`ではなく`Ctrl`を使用）で、直近の編集操作を取り消し・やり直しできる。追加・削除・並び替え・ピン留め切り替え・一括インポート等の構造的な操作は1操作ごとに1つのUndo単位として記録する。タイトル・お知らせ本文・カテゴリ名・リンク名/URL等のテキスト入力は、フォームが再描画されるまでの連続した入力をまとめて1つのUndo単位とする（1文字ごとに記録すると実用にならないため）。**この操作履歴はブラウザタブ内だけの一時的な状態であり、保存・永続化は一切行わない**（ページの再読み込みで消える。§1.3参照）。最大50件まで保持する |
| アイコン選択 | アイコン欄クリックでパレット（30種類の絵文字）をポップアップ表示し選択。パレット外クリックで自動的に閉じる |
| ライブプレビュー | `config`が変更されるたびに、閲覧画面相当のHTML/CSSを組み立てて`iframe.srcdoc`に反映（実際のサーバーレンダリングとは別ロジックで簡易再現） |
| プレビュー表示切替 | 「💻 PC」「📱 スマホ」ボタンで`iframe`の表示サイズを切り替え（PC: 100%幅、スマホ: 375×750pxの端末フレーム風表示） |
| リンクチェック | 「🔗 リンクをチェック」ボタンで、画面に表示中の全リンクのURLを`POST /api/check-links`に送信し、到達可否を確認する。結果は各リンク行にバッジ（✅/❌、❌はエラー内容をtitle属性で表示）として反映される。**結果はDBに保存されず、この編集セッション中のみ**表示される。リンクの追加・削除・並び替え・カテゴリの追加・削除・並び替えを行うと、インデックスのずれを避けるため表示中の結果は破棄される |
| 保存 | 「💾 保存」ボタンで現在の`config`を`PUT /api/config`に送信する。`currentVersion`を`If-Match`ヘッダーとして送信し、サーバー側で他の変更と競合していないか検証する（§1.2, §4.2）。バリデーション成功かつ競合なしの場合は返却データで`config`と`currentVersion`を更新し、成功アラートを表示。バリデーションエラー時はエラー内容をアラート表示し`config`は変更しない。**競合（`409`）時**は競合内容をアラート表示した上でサーバーの最新内容を自動的に再取得し、編集中の内容を破棄する |
| JSONエクスポート | 現在の`config`を`{version, exportDate, config}`形式のJSONファイルとしてダウンロード（クライアント側のみで完結、DBへの影響なし）。`version`はこのエクスポート形式自体のバージョン（`EXPORT_FORMAT_VERSION`）であり、本システムのバージョン（本書冒頭記載）とは独立している |
| JSONインポート | エクスポート形式のJSONファイルを読み込み、クライアント側で簡易バリデーション（§5.2.3参照）した上で`config`を置き換える。**この時点ではDBには反映されない**。反映するには別途「保存」を押す必要がある |
| 変更履歴 | 「📜 変更履歴」ボタンでモーダルを開き、`GET /api/history`（§4.7）で一覧（保存日時の新しい順）を取得して表示する。各行の「この内容を確認」ボタンで`GET /api/history/:id`（§4.7）を呼び出し、確認ダイアログの上でその時点のスナップショットを`config`に読み込む。**この時点ではDBには反映されない**。反映するには別途「保存」を押す必要がある（JSONインポートと同じ流れ） |
| 名前順に並び替え | 「🔤 名前順に並び替え」ボタンで、カテゴリ名・各カテゴリ内のリンク名をそれぞれ`localeCompare("ja")`で一括ソートする。**この時点ではDBには反映されない**。反映するには別途「保存」を押す必要がある |
| アクセス統計 | 「📊 アクセス統計」ボタンでモーダルを開き、現在読み込み中の`config`（クリック数を含む）と`GET /api/history`（§4.7）から、カテゴリ数・リンク数・総クリック数・保存履歴件数・よく使われているリンクTOP10を集計して表示する。新規のAPIエンドポイントは使わず、既存のデータのみで完結する（表示専用。ここでの操作による`config`への影響はない） |
| DBダウンロード | 「💾 DBをダウンロード」ボタンで`GET /api/backup/download`（§4.8）へ遷移し、現在の`data/portal.db`をそのままダウンロードする |
| デフォルトに戻す | 確認ダイアログの上で `POST /api/config/reset` を呼び出し、DBを`portal-config.json`のシードデータにリセットする。取り消し不可 |

#### 5.2.3 クライアント側インポートバリデーション（`handleImportFile()`）
| 項目 | 検証内容 | 不正時の挙動 |
|---|---|---|
| `data.config` | 存在すること | エラーとして処理を中断 |
| `data.config.title` | 文字列であること | エラーとして処理を中断 |
| `data.config.categories` | 配列であること | エラーとして処理を中断 |
| `data.config.themeColor` | `^#[0-9A-Fa-f]{6}$` に一致すること | 不一致時は警告ログを出し、`#00a968`に補正（処理は継続） |
| `news` | 配列でない場合は`[{date:"News", text:<値>}]`に、未定義の場合は`[]`に補正 | 処理は継続 |
| `categories[].name` | 未設定の場合 `"未設定"` に補正 | 処理は継続 |
| `categories[].links` | 配列でない場合は`[]`に補正 | 処理は継続 |

なお、インポートされた内容は「保存」時にサーバー側バリデーション（§6）で再検証されるため、クライアント側のチェックをすり抜けた不正なURL等はサーバー側で拒否される。

---

## 6. バリデーション仕様（`src/validate.ts`）

`PUT /api/config` 実行時、また `deno task build` からの `scripts/check-config.ts` 実行時（`src/portal-config.json` に対して）、
以下を満たさない場合はエラーとして拒否する（`validateConfig()`）。

| 項目 | 検証内容 |
|---|---|
| `title` | 文字列であり、かつ空でないこと |
| `themeColor` | `^#[0-9A-Fa-f]{6}$` に一致すること（例: `#00a968`） |
| `categories` | 配列であること |
| `categories[].name` | 文字列であること |
| `categories[].links` | 配列であること |
| `categories[].hidden` | 指定する場合は真偽値であること（省略可。省略時はfalse=公開扱い） |
| `categories[].links[].name` | 文字列であること |
| `categories[].links[].icon` | 文字列であること |
| `categories[].links[].url` | §7.2「URL検証仕様」を満たすこと |
| `news` | 配列であること（省略時は空配列として扱う） |
| `news[].date` / `news[].text` | いずれも文字列であること |
| `news[].pinned` | 指定する場合は真偽値であること（省略可。省略時はfalse扱い） |
| `news[].expiresAt` | 指定する場合は`^\d{4}-\d{2}-\d{2}$`形式の文字列であること（省略可・空文字列可。省略・空文字列は無期限を表す） |

いずれかの検証に失敗した場合、当該項目を特定するメッセージ（例: `カテゴリ[0]のリンク[0]のURLが不正です`）とともに例外を投げ、
APIは`400`を返す（ビルド前チェックの場合はビルドを中断する）。

---

## 7. セキュリティ仕様

### 7.1 XSS対策
- 閲覧画面・編集画面ともに、ユーザー入力に由来する値（タイトル・お知らせ・カテゴリ名・リンク名・アイコン・URL文字列）をHTMLに埋め込む際は、必ず`escapeHtml()`関数（`&<>"'`をHTMLエンティティに変換）を通す
- `escapeHtml()`は両画面それぞれの`<script>`内に同一実装を保持する（共有モジュール化はしていない）

### 7.2 URL検証
- **フロントエンド**（`src/client/portal.ts` `sanitizeUrl()`）: `http://`または`https://`で始まるURL、あるいは`/`または`.`で始まる相対パスのみそのまま許可。それ以外（`javascript:`スキーム等）は`#`に置き換える
- **バックエンド**（`src/validate.ts` `isValidUrl()`）: 同等の基準（`^https?:\/\//i` または `^[./]`）で検証し、満たさない場合は`PUT /api/config`を`400`で拒否する

### 7.3 認証・アクセス制御
- 本システムには**認証機能は実装されていない**
- `PUT /api/config`・`POST /api/config/reset`・`POST /api/check-links` は、APIにアクセスできる者であれば誰でも実行可能
- そのため、**信頼できるネットワーク内（社内LANなど）でのみ運用すること**を前提とする
- インターネットに公開する場合は、リバースプロキシでのBASIC認証やVPN経由でのアクセス制限など、システム外側での保護を別途講じること（本システム自体には実装しない）

### 7.4 `POST /api/check-links` のSSRFに関する注意
このエンドポイントは、リクエストで指定された任意のURLへサーバーがHTTPリクエストを送信する（§4.4）。
認証がない前提（§7.3）と合わせると、APIにアクセスできる者は社内ネットワーク内の他ホスト（`http://`で始まる
プライベートIP・localhost等）に対してサーバーを踏み台にした調査リクエストを送らせることが可能である。
§7.3と同じく信頼できるネットワーク内での運用を前提とし、それ以上の対策（宛先ホストの許可リスト化等）は
現時点では実装していない。

### 7.5 入力サイズ制限
§4.9参照。

### 7.6 `GET /api/backup/download` に関する注意
§4.8参照。認証がないため、このエンドポイントにアクセスできる者はDB全体をダウンロードできる。§7.3と同じく
信頼できるネットワーク内での運用を前提とする。

---

## 8. 非機能仕様

| 項目 | 仕様 |
|---|---|
| 対応Denoバージョン | v2.9以上 |
| ソースの正本 | TypeScript（`src/`配下）。JavaScript（`dist/`・`public/js/`配下）は `deno task build` によるビルド生成物であり、直接編集しない |
| 型チェック | `deno task check`（`deno check`）で`strict`モードの型チェックを実施する |
| ポート | 環境変数`PORT`で指定可能。既定値は`3000` |
| データ永続化 | SQLiteファイル（`data/portal.db`）。プロセス再起動後もデータは保持される |
| SQLiteドライバ | `jsr:@db/sqlite`（FFI経由でネイティブsqlite3を利用。`--allow-ffi`が必要） |
| 同時実行 | `Database`は同期API。単一プロセス内での逐次アクセスを前提とし、大規模な同時書き込み負荷は想定していない。同時編集による上書き事故は`version`列を用いた楽観的排他制御（§1.2, §4.2）で検知する |
| ロギング | 全リクエストについて `<ISO日時> <メソッド> <パス> <ステータス> <処理時間ms>` 形式のアクセスログを、ハンドラ内で例外が発生した場合は `<ISO日時> ERROR <メソッド> <パス> - <エラーメッセージ>` 形式のエラーログを、標準出力と`data/access.log`（追記）の両方に出力する（`src/server.ts` `logLine()`）。ログ書き込み自体の失敗はリクエスト処理に影響させない（握りつぶす） |
| DBバックアップ | §2.3参照 |
| DBダウンロード | §4.8参照 |
| ヘルスチェック | §4.5参照 |
| リンクの定期自動チェック | §2.4参照 |
| ブラウザ対応 | モダンブラウザ（`fetch`、CSS変数、`prefers-color-scheme`、HTML5 Drag and Drop APIに対応したもの） |

---

## 9. 用語集

| 用語 | 説明 |
|---|---|
| 設定（config） | タイトル・テーマカラー・お知らせ・カテゴリ・リンクをまとめた1つのJSONオブジェクト。システム全体で単一のみ存在する |
| シードデータ | 初回起動時にDBが空の場合に投入される初期設定（`src/portal-config.json`）。DB作成後は「デフォルトに戻す」操作を行わない限り参照されない |
| カテゴリ | リンクをグループ化する単位。名称と表示順、複数のリンクを持つ |
| リンク | カテゴリに属する個別の外部/内部URL。名称・URL・アイコンを持つ |
| 正本（ソースオブトゥルース） | 人間が直接編集する原本のファイル。本システムでは仕様の正本は本ファイル（SPEC.md）、実装の正本は`src/`配下のTypeScriptファイルを指す |
| 楽観的排他制御 | 保存時に「自分が読み込んだ時点のバージョン」をサーバーに提示し、その後に他の変更が入っていないかを確認する競合検知方式。ロックは取得せず、競合時のみ`409`で通知する（§1.2, §4.2） |
| バージョン（`settings.version`） | 設定（config）が何回更新されたかを表す整数。`PUT /api/config`のたびに1つ進み、`GET`/`PUT`のレスポンスヘッダー`ETag`として公開される。**下記の「開発版バージョン」「安定版バージョン」（本システム自体のバージョン、§1.4）とは別物**なので混同しないこと |
| 開発版バージョン（`v0.N`） | 本システム自体のバージョン番号のうち、本ファイルへの変更すべてに付与する連番（§1.4.1）。Nは1から始まり、いかなる理由でもリセットしない。§10の変更履歴に対応する |
| 安定版バージョン（`vX.Y`） | 開発版バージョンの中から、安定していると判断した時点を選んでリリース用に切り出した番号（§1.4.2）。X=安定版リリースの通し番号、Y=切り出し時点の開発版バージョンのN。§11の安定版リリース履歴に対応する |
| リリースポリシー | 「安定している」の判定基準・リリース手順・不具合発見時の扱いを定めた方針（§1.5）。本システムは社内単一環境での運用のため、安定版のみをリリース対象とする |
| クリック集計 | リンクごとの累計クリック回数を、閲覧者・端末を識別せずURL単位で匿名集計する機能（`link_clicks`テーブル、§3.5, §4.6）。閲覧画面のクリックで加算され、両画面で表示される |
| 変更履歴（`change_log`） | 設定が保存されるたびの内容スナップショットの記録（§3.8, §4.7）。誰が変更したかは記録せず、「いつ・どんな内容だったか」のみを保持する。直近50件のみ保持 |
| バックアップ | `data/portal.db`を`data/backups/`へ定期的（既定60分間隔）にコピーする仕組み（§2.3）。直近24世代を保持し、リストアは手動操作を想定する |
| ピン留め（お知らせ） | 特定のお知らせ（`news[].pinned`）を常に先頭に表示する機能（§3.2, §5.1.2）。編集者が管理する共有データであり、閲覧者個人の状態ではないため§1.3のLocalStorage禁止制約には抵触しない |
| NEWバッジ | 保存日時（`link_added_at`テーブル、§3.6）が直近7日以内（`NEW_BADGE_DAYS`）のリンクに閲覧画面で表示される赤いバッジ（§5.1.2） |
| 元に戻す/やり直す（Undo/Redo） | 編集画面での操作取り消し・やり直し機能（§5.2.2）。ブラウザタブ内だけの一時的な操作履歴であり、保存・永続化は一切行わない（§1.3） |
| 一括インポート | 編集画面で「名称, URL」形式のテキストを複数行貼り付け、まとめてリンクを追加する機能（§5.2.2）。JSONインポート（設定全体の置き換え）とは異なり、選択したカテゴリへのリンク追加のみを行う |
| 下書きカテゴリ | `categories[].hidden`が`true`のカテゴリ（§3.3）。閲覧画面には表示されないが、編集画面では引き続き編集できる（§5.1.2, §5.2.2） |
| 到達不可バッジ | サーバー側の定期自動チェック（§2.4）で到達できなかったリンクに閲覧画面で表示される⚠️バッジ（`link_check_status`テーブル、§3.7, §5.1.2）。編集画面の「🔗 リンクをチェック」（手動・その場限り）とは別の仕組み |
| アクセス統計 | 編集画面のモーダルで、カテゴリ数・リンク数・総クリック数・保存履歴件数・よく使われているリンクTOP10を表示する機能（§5.2.2）。新規のAPIは使わず既存のデータを集計するのみで、表示専用（設定を変更しない） |
| DBダウンロード | 編集画面から現在の`data/portal.db`をそのままダウンロードする機能（`GET /api/backup/download`、§4.8, §5.2.2）。自動バックアップ（§2.3）とは別に、リスクのある変更前に手元へ控えを取る用途を想定している |

---

## 10. 変更履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| v0.21 | 2026-09-03 | 機能改良5点を追加。①カテゴリの下書き状態(`categories.hidden`。閲覧画面には表示せず編集画面では引き続き編集可能。§3.3, §5.1.2, §5.2.2)、②DBの手動バックアップダウンロード(`GET /api/backup/download`。§4.8, §5.2.2)、③カテゴリ/リンクの名前順一括ソート(§5.2.2)、④アクセス統計モーダル(既存データの集計表示のみ、新規APIなし。§5.2.2)、⑤リンクの定期自動チェック(新テーブル`link_check_status`・環境変数`LINK_CHECK_INTERVAL_MINUTES`・閲覧画面の到達不可バッジ。§2.4, §3.7, §5.1.2)を実装。§3.7「link_check_status」の新設に伴い`change_log`(§3.7→§3.8)・論理データ構造(§3.8→§3.9)・共有型定義(§3.9→§3.10)の節番号が1つずつ繰り下がった。§6にcategories[].hiddenのバリデーション規則を、§7に新設エンドポイントの認証なしに関する注意(§7.6)を追加 |
| v0.20 | 2026-09-03 | 機能改良6点を追加。①お知らせのピン留め(`news.pinned`。常に先頭表示。§3.2, §5.1.2, §5.2.2)、②リンクのNEWバッジ(`link_added_at`テーブル新設・直近7日以内のリンクに表示。§3.6, §5.1.2)、③お知らせの有効期限(`news.expiresAt`。閲覧画面でのみ期限切れを自動非表示。§3.2, §5.1.2, §5.2.2)、④編集画面でのカテゴリ横断ドラッグ&ドロップ(従来の同一カテゴリ内限定から拡張。§5.2.2)、⑤一括インポート(「名称, URL」形式のテキスト貼り付けで複数リンクを一括追加。§5.2.2)、⑥編集画面のUndo/Redo(セッション内限定、保存・永続化はしない。§1.3, §5.2.2)を実装。DBスキーマは`news`に`pinned`/`expires_at`列を追加し、新テーブル`link_added_at`を新設(§3.2, §3.6)。既存の`change_log`(§3.6→§3.7)・論理データ構造(§3.7→§3.8)・共有型定義(§3.8→§3.9)の節番号が1つずつ繰り下がった。§6にpinned/expiresAtのバリデーション規則を追加 |
| v0.19 | 2026-09-03 | §1.5.1のリリース基準を明確化。「既知の未修正バグが0件であること」に加え、発見した不具合はバックログとして保留・記録せず、リリース前提でその場で修正する方針を明記した（「軽微だから後回しにする」という判断はしない。未修正バグを一覧などで追跡・保留する仕組みは持たない）。§1.5.3にも同様の記述を反映した |
| v0.18 | 2026-09-03 | ドキュメント整合性の確認・修正。§9用語集に「開発版バージョン」「安定版バージョン」「リリースポリシー」を追加し、既存の「バージョン（`settings.version`）」エントリと混同しないよう注記を追加。README.mdに§1.4・§1.5・§11へのリンクを追加。SPEC.md内の全`§`参照・コード中の`SPEC.md §`コメント・DBスキーマ(§3)・APIエンドポイント(§4)・ディレクトリ構成(§2)・`deno.json`のタスク定義(§2.2)・`.gitignore`の対象範囲(§2)が実装と一致していることを確認した（実装への変更なし） |
| v0.17 | 2026-09-03 | リリースポリシーを新設（§1.5）。安定版と判定する基準、リリースの手順、不具合発見時の扱いを明文化。§11「安定版リリース履歴」を新設（現時点ではエントリなし）。§7.5の参照先が旧番号（§4.5）のまま更新漏れになっていたバグを修正し、正しい§4.8に訂正 |
| v0.16 | 2026-09-03 | 安定版バージョン`vX.Y`の方針を追加（§1.4.2）。リリースは安定版のみを対象とする方針、X(安定版リリースの通し番号)・Y(切り出し時点の開発版N)の定義を明文化 |
| v0.15 | 2026-09-03 | §1.4に「Nはいかなる理由でもリセットしない」ことを明記。将来先頭の`0`を`1`に切り替える場合もNはその時点の値から継続することを明文化 |
| v0.14 | 2026-09-03 | バージョニング方針を新設（§1.4）。SemVer風の`MAJOR.MINOR`表記（旧`1.0`〜`5.4`）を廃止し、変更の重大さを区別しない単純な累積連番`v0.N`表記に移行。本表の既存全エントリの表記をこの回で遡って統一した（内容・実施日は変更していない） |
| v0.13 | 2026-09-03 | 認証機能を除く運用・機能改善8点を追加。①アクセスログ/エラーログを`data/access.log`と標準出力へ記録(§8)、②`data/portal.db`の定期バックアップ(既定60分間隔、直近24世代保持。§2.3)、③死活監視用`GET /healthz`(§4.5)、④リンクの匿名クリック数集計(`link_clicks`テーブル・`POST /api/click`・両画面での表示。§3.5, §4.6)、⑤保存のたびのスナップショットを記録する変更履歴(`change_log`テーブル・`GET /api/history`・`GET /api/history/:id`・編集画面の履歴モーダルからの内容確認/読み込み。直近50件保持。§3.6, §4.7, §5.2.2)、⑥閲覧画面の検索結果ハイライト表示(`<mark>`タグ。§5.1.2)、⑦閲覧画面のカテゴリ折りたたみ表示(DOM上の一時的な表示のみで永続化はしない。§1.3, §5.1.2)、⑧編集画面でのカテゴリ/リンクのドラッグ&ドロップ並び替え(既存の⬆️⬇️ボタンと併存。リンクは同一カテゴリ内のみ。§5.2.2)を実装。`LinkItem`に読み取り専用の任意項目`clicks`を追加(§3.7, §3.8) |
| v0.12 | 2026-09-03 | 「保存の楽観的排他制御」と「リンクチェック」を追加。`settings`テーブルに`version`列を追加し、`GET/PUT/POST reset`の`ETag`/`If-Match`ヘッダーでバージョンを検証、競合時は`PUT`が`409`を返す仕様に変更(§1.2, §4.2)。`POST /api/check-links`エンドポイントを新設し、編集画面から画面上の全リンクの到達可否をその場でチェックできるようにした(DBには保存しない。§4.4, §5.2.2)。§7に`check-links`のSSRF注意事項を追記 |
| v0.11 | 2026-09-03 | §1に「バックエンドの実行環境は生成後のJavaScript(`dist/server.js`)である」ことを明文化。`deno task dev`によるTypeScript直接実行は開発時の利便機能であり本番運用の方式ではないことを明記(実装・ビルド構成自体は変更なし) |
| v0.10 | 2026-09-03 | 「ピン留めリンク」機能を削除。`LinkItem.pinned`、DBの`links.pinned`列、閲覧画面のピン留め欄、編集画面のピン留め切り替えボタンをすべて撤去し、v0.8以前の構造に戻した。§1.3(LocalStorage禁止制約)自体は維持するが、「実現例」として記載していたピン留めへの言及は一般的な考え方の説明に置き換えた |
| v0.9 | 2026-09-03 | バックエンドを復活。静的ホスティング方式(v0.5〜v0.8)を廃止し、Deno + TypeScript + SQLiteのサーバー(`src/server.ts`・`src/db.ts`)とREST APIを再導入。`src/portal-config.json`は「設定の正本」から「初回起動時のシードデータ」に役割変更。編集画面(`edit.html`)はファイル生成方式からDB保存(`PUT /api/config`)方式に戻し、v0.5〜v0.8 で追加したピン留めリンク機能はDBスキーマ(`links.pinned`列)に統合して維持。バックエンドの新規コードもTypeScript実装+JavaScript生成のビルド方式を踏襲 |
| v0.8 | 2026-09-03 | 「ピン留めリンク」機能を追加。`LinkItem`に任意項目`pinned`(真偽値)を新設し、`true`のリンクを閲覧画面最上部に共通表示する。§1.3のLocalStorage禁止制約と両立する代替案として、個人ごとの状態ではなく編集者が管理する共有データとして実装。編集補助ツールにピン留め切り替えボタンを追加 |
| v0.7 | 2026-09-03 | §1.3を新設し、LocalStorage・sessionStorage・IndexedDB・Cookie等ブラウザ内永続化に依存する仕様・機能を明示的に禁止事項として明文化。お気に入り・クリック履歴・ダークモード手動切り替え等の「将来拡張候補」記載を削除し、この制約下では実装しない方針を明確化 |
| v0.6 | 2026-09-03 | 設定データ・フラットファイルの形式をTypeScript(`src/portal-config.ts`)からJSON(`src/portal-config.json`)に変更。構造検証は`src/types.ts`の型キャスト+ビルド前バリデーション(`scripts/check-config.ts`、`deno task build`から自動実行)で担保する方式に変更。編集補助ツールが生成するファイルもTypeScriptからJSONに変更 |
| v0.5 | 2026-09-03 | 実行環境を静的ホスティングに変更。サーバー(`src/server.ts`)・SQLiteデータベース(`src/db.ts`)・REST APIを廃止し、設定データをソースコード内のフラットファイル(`src/portal-config.ts`)へ変更。閲覧画面はビルド時に設定を静的バンドルする方式に変更し、編集画面はサーバー保存を行わないローカル編集補助ツール(TypeScriptファイル生成)に再設計 |
| v0.4 | 2026-09-03 | ランタイムをNode.js(Express)からDenoに移行し、実装言語をTypeScriptに統一。`src/`配下のTypeScriptを正本とし、`deno bundle`でJavaScript(`dist/`・`public/js/`)を生成するビルド方式を導入。SQLiteドライバを`node:sqlite`から`jsr:@db/sqlite`に変更 |
| v0.3 | 2026-09-03 | SQLiteデータベース + Expressバックエンドを導入。マスター仕様書（本ファイル）を新設し、仕様をREADMEから分離・集約 |
| v0.2 | 2026-02-27 | LocalStorage自動保存、JSONインポート/エクスポート、デフォルトに戻す機能を追加（静的サイト構成時代の仕様。現行仕様には非適用） |
| v0.1 | 2026-02-14 | 初回リリース（静的サイト構成。`data.js`手動編集方式） |

---

## 11. 安定版リリース履歴

§1.5「リリースポリシー」に基づき安定版として切り出したリリースを記録する（§1.4.2の`vX.Y`）。
開発版の変更履歴（§10）とは別の表であり、§10の全エントリがここに載るわけではない。

| バージョン | 日付 | 対応する開発版 | 内容概要 |
|---|---|---|---|
| （まだ安定版リリースなし） | | | |
